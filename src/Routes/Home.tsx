import styled from 'styled-components';

function Home() {
  return (
    <HomeContainer>
      <p>Home</p>
    </HomeContainer>
  );
}

const HomeContainer = styled.div`
  background-color: white;
  height: 200vh;
`;

export default Home;
